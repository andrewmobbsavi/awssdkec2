# Base Docker and Docker Compose Repo for AWS CDK

## Basic setup
* Clone the repo:

* `git clone git@github.com:andrewmobbsavi/awssdkec2.git`
* `docker-compose up -d --build`
* `docker exec -it awscdkec bash`
* `cdk --version`
